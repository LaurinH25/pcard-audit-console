# Copy to .streamlit/secrets.toml for local testing. Never commit secrets.toml.
# On Streamlit Community Cloud, paste this same content into
# App settings -> Secrets instead -- it is not read from this file there.

GEMINI_API_KEY = "..."
GEMINI_MODEL = "gemini-3.6-flash"
